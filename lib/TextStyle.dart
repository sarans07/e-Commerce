


import 'dart:ui';

import 'package:flutter/material.dart';

TextStyle pagestyle = TextStyle(color: Colors.black87,fontSize: 20,fontWeight: FontWeight.bold,
  shadows: [
    Shadow(
        offset: Offset(3.0, 3.0),
        blurRadius: 5.0,
        color: Colors.grey
    )
  ],
);
TextStyle headings = TextStyle(color: Colors.black,fontSize: 23,fontWeight: FontWeight.bold,
  shadows: [
    Shadow(
        offset: Offset(3.0, 3.0),
        blurRadius: 5.0,
        color: Colors.grey
    )
  ],
);