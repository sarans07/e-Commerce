


import 'dart:ui';

import 'package:flutter/material.dart';

TextStyle pagestyle = TextStyle(color: Colors.black87,fontSize: 18,fontWeight: FontWeight.bold,
  shadows: [
    Shadow(
        offset: Offset(1.0, 1.0),
        blurRadius: 1.0,
        color: Colors.grey
    )
  ],
);
TextStyle headings = TextStyle(color: Colors.black,fontSize: 24,fontWeight: FontWeight.bold,
  shadows: [
    Shadow(
        offset: Offset(1.0, 1.0),
        blurRadius: 1.0,
        color: Colors.grey
    )
  ],
);